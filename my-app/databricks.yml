bundle:
  name: my-app

sync:
  include:
    - .build

artifacts:
  default:
    build: uv run apx build

resources:
  apps:
    my-app-app:
      name: "my-app"
      description: "my-app created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true